model{
	
	surv ~ dunif(0.5, 0.95)

  mu ~ dunif(1,1000000)
  sd ~ dunif(1,1000000)

  # Adult numbers
  
	for(i in 1:years) {

	  Nadult[i] ~ dnorm(mu, 1/(sd^2)) T(0, 1e+10)

		TruePairs[i] ~ dbinom((RObase[i]*(surv^AgeDif[i]))/(Nadult[i]), Pair_viable_count[i])

	}


}